{
    "android": {
      "platformName": "${APPIUM_PLATFORM_NAME}",
      "automationName": "${APPIUM_AUTOMATION_NAME}",
      "deviceName": "${APPIUM_DEVICE_NAME}",
      "udid": "${APPIUM_UDID}", 
      "appPackage": "${APPIUM_APP_PACKAGE}",
      "appActivity": "${APPIUM_APP_ACTIVITY}",
      "noReset": "${APPIUM_NO_RESET}"
    },

    "ios": {
      "platformName": "${APPIUM_PLATFORM_NAME}",
      "automationName": "${APPIUM_AUTOMATION_NAME}",
      "deviceName": "${APPIUM_DEVICE_NAME}",
      "udid": "${APPIUM_UDID}",
      "bundleId": "${APPIUM_APP_PACKAGE}",
      "noReset": "${APPIUM_NO_RESET}"
    }
  }